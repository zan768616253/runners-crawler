/// ?取文件?里面的文件，并根据['size', 'atime', 'mtime', 'ctime', 'birthtime']中的?性?行排序
/// BY CHC  

var fs = require('fs');
var path = require('path');

/// @dirpath:文件?路?
/// @sortprop:排序?性，默?mtime
var filelist = function (dirpath, sortprop){

}